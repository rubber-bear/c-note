/*
二维数组
  int a[3][5] -》3行5列
  通常理解为a是一个3行5列的矩阵

二维数组的遍历
  for (i=0; i<3; i++) {
    for (j=0; j<5; J++) {
      a[i][j] = i*j
    }
  }
  a[i][j] 是一个int 表示第i行第j列上的单元

二维数组的初始化
  int a[][5] = {
    {0, 1, 2, 3,4},
    {2, 3, 4, 5, 6},
  };
  列数必须给出，行数可以由编译器来数
  每行一个{}，逗号分隔
  最后的逗号可以存在
  如果省略表示补0

eg：
  tic - tac - toe 游戏
  读如一个3X3的矩阵，矩阵中的数字为1表示该位置上有一个X，为0表示为O
  程序判断这个矩阵中是否有获胜的一方，输出表示获胜一方的字符X或者O，或输出无人获胜
*/






